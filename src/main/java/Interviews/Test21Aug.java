package Interviews;

public class Test21Aug {
    public static void main(String[] args) {

        //abcd123bnbn23sdf - (n)
        //charAt(i)-'0' = [0-9]  if 0 = 48 , if 9 = 57
        //[1-1, 2-2, 3-2] - (logN)
        // polling -  o(n) - 9 digits
        //1-size of heap
        // polling - last element = max
        // polling - max(m2 , max)
        //JBPM
        System.out.println(1%2 + " "+ 2%1);
    }
}
